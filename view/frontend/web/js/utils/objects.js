define([
    'jquery'
], function ($) {
    'use strict';

    $.breezemap.__register('mage/utils/objects', 'mage/utils/objects');
    $.breezemap.__register('mage/utils/objects');
    $.breezemap.__register('./objects', 'mage/utils/objects');
});
